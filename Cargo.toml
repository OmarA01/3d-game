[package]
name = "crystal-rush"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = { version = "0.4", default-features = false, features = ["audio"] }

[profile.release]
opt-level = 3
lto = true
strip = true
